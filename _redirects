# 1️⃣ Serve all .html files without showing .html in the URL
/*   /:splat.html   200

# 2️⃣ Redirect any URL ending with .html to clean URL (updates browser URL)
*.html   /:splat   301!

# 3️⃣ Optional: force trailing slash consistency (redirect /about → /about/)
# Remove this if you prefer URLs without trailing slashes
/*   /:splat/   301
/*   /:splat/   301


/en       /index_angol.html  200
/en/*     /index_angol.html  200
/hu       /index.html  200
/hu/*     /index.html  200

